export type FlexProps = {
  direction?: 'row' | 'col',
  gap?: number | string,
  justify?: 'fe' | 'fs' | 'c' | 'sb' | 'sa',
  align?: 'fe' | 'fs' | 'c',
  fullWidth?: boolean,
  fullHeight?: boolean,
  wrap?: boolean,
  autoGap?: boolean,
}
